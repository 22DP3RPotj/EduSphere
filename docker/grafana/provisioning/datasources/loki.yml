# yaml-language-server: $schema=https://raw.githubusercontent.com/grafana/grafana/refs/heads/main/conf/provisioning/datasources/sample.yaml

apiVersion: 1

datasources:
  - name: Loki
    uid: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: true
